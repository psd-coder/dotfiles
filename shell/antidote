# Load antidote (use HOMEBREW_PREFIX set in platform file to avoid slow brew --prefix)
ANTIDOTE_PATH="${HOMEBREW_PREFIX:-/opt/homebrew}/opt/antidote/share/antidote/antidote.zsh"
if [[ -f "$ANTIDOTE_PATH" ]]; then
  source "$ANTIDOTE_PATH"
  antidote load
fi
