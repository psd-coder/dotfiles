#!/bin/zsh

# import helpers
source `dirname $0`/helpers

# Install brew
function setup_os {
  einf "Configuring macOS"
  sudokeepalive
  sh $DOTFILES_SHELL/macos

  reload_shell
}

askdo setup_os "Setup os?"
